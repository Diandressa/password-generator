import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    inputer: {
        backgroundColor: '#f803c3',
        width: '100%',
        color: '#fff',
        fontSize: 20,
        borderWidth: 1,
        borderColor: '#d2e716',
        borderRadius: 5,
        textAlign: 'center',
    }
});