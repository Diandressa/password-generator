import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    inputer: {
        backgroundColor: '#f803c3',
        width: '100%',
        color: '#fff',
        fontSize: 25,
        borderRadius: 5,
        textAlign: 'center',
    }
});